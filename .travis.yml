language: rust
cache: cargo

rust:
  - stable
  - beta
  - nightly

matrix:
  allow_failures:
    - rust: nightly

os:
  - linux

services:
  docker

before_install:
  - docker pull pmem/pmemkv
  - docker run pmem/pmemkv /bin/sh

script:
  - cargo build --verbose --all
  - cargo test --verbose --all
